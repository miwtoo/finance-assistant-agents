# Domain Model

This project's domain is defined in a single file:

- **Root CONTEXT.md** — [`CONTEXT.md`](../../CONTEXT.md)

This is the canonical source for all domain terminology, boundaries, and glossary terms. Do not duplicate domain definitions elsewhere.

## Architecture Decision Records

ADRs are stored in [`docs/adr/`](../adr/). Each ADR captures a significant architectural decision, its context, and rationale. Follow the numbering convention `NNNN-title.md`.
